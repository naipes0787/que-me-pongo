{{#partial "contenido"}}

<div>
    <h2>
        Guardarropas: {{ nombre }}
    </h2>

    {{#each prendas}}
    <div class="prenda">
        <img src="data:image/png;base64, {{foto}}" alt="{{ nombre }}" />
        <div class="detalles">
            <h2>{{nombre}}</h2>
            <p>Color: {{ colorPrincipal.nombre }}</p>
            <p><a class="btn btn-info" href="/prendas/{{id}}">Ver Más</a></p>
        </div>
    </div>
    {{/each}}
</div>

{{/partial}}

{{>layout.hbs}}